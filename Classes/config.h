#pragma once

enum { DEAD , ALIVE};

enum {PHASE_DEF,AIMING, RAPID, LASER, PHASE_DEAD};

enum { LEFT, RIGHT , UP, DOWN};

enum { JUMPSTATE_DEF, JUMPSTATE_UP, JUMPSTATE_DOWN };




#define DIR_LEFT -1
#define DIR_RIGHT 1

